import React from 'react';
import FliprLogo from '../../assets/svgs/logo.svg';

const logo = (props) => {
    return <span>
        <img alt="logo" src={FliprLogo} />
    </span>
}

export default logo;