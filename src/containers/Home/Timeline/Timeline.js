import React from 'react';

const Timeline = (props) => {
    console.log(props.selectedData);
    return (
        <React.Fragment>
            <div>asdf</div>
        </React.Fragment>
    );
};

export default Timeline;